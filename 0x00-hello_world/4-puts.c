#include <stdio.h>

/**
 * main - Prints a message using the puts function
 * Return: 0
 */
int main(void)
{
	char message[100] = "\"Programming is like building a multilingual puzzle";

	puts(message);
	return (0);
}
